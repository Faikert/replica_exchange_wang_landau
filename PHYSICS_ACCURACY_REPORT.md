# Оценка физической точности Wang–Landau

Дата проверки: 15 июля 2026 года.

## Вердикт

**Физическое ядро дипольной Ising-модели реализовано корректно, но текущий результат Apamea непригоден для научной интерпретации или публикации.** Формула пары, знак энергии, `ΔE`, minimum-image convention, cutoff-геометрия и инкрементальные поля проходят имеющиеся тесты. Независимый пересчёт геометрии Apamea точно воспроизводит число соседей и исходную энергию локального Metropolis-расчёта.

Проблема находится на уровне дискретизации энергии и статистической сходимости DOS:

- Apamea завершён по лимиту, `converged=false` после 800 млн попыток;
- оконные DOS сильно несовместимы в overlap-областях;
- относительно Metropolis-референса RMSE энергии составляет 900.6, а максимум ошибки `C/N` — 1.199;
- WL предсказывает пик `C/N=1.289` при `T=1.094`, Metropolis — `C/N=0.379` при `T=1.788`;
- для малой системы 2×2×2 редкие численные переходы в соседние бины на точных границах энергии не дают histogram стать flat даже после 5 млн попыток на walker.

До исправления перечисленных ниже проблем файлы `apamea_dos.csv` и `apamea_thermo.csv` следует считать диагностическими, а не физическим результатом.

## Проверка модели

Реализованный гамильтониан:

```text
J_ij = D [e_i·e_j - 3(e_i·r̂_ij)(e_j·r̂_ij)] / r_ij³
E    = Σ_{i<j} J_ij σ_i σ_j
h_i  = Σ_j J_ij σ_j
ΔE_i = -2 σ_i h_i
```

Знак проверен на аналитических случаях:

- соосные диполи вдоль `r`: `J=-2D/r³`;
- параллельные диполи поперёк `r`: `J=+D/r³`;
- при удвоении расстояния модуль уменьшается в восемь раз.

`DenseCouplings` и `CsrCouplings` хранят симметричные `J_ij`. После flip старого спина `σ_k` поля обновляются на `-2σ_k J_ik`, что соответствует новому значению `σ_k'=-σ_k`. Полная энергия вычисляется как `0.5 Σ_i σ_i h_i`. Эти соглашения взаимно согласованы.

Периодические условия используют minimum image отдельно по каждой положительной длине box. Это корректно для заявленной усечённой ортогональной модели, но **не является Ewald-суммированием бесконечного дипольного кристалла**. При `box_z=0` направление `z` открыто.

### Независимая проверка Apamea

Геометрия `examples/apamea/apamea_N720.csv` была независимо прочитана и пересчитана по той же физической формуле, без использования C++-классов проекта.

| Величина | Независимый расчёт | `apamea_N720_0.out` |
|---|---:|---:|
| Спины | 720 | 720 |
| Нормы локальных осей | ровно 1 | единичные оси |
| Cutoff | 5.1 | 5.1 |
| Среднее число соседей | 103.2 | 103.2 |
| Энергия состояния `σ_i=+1` | −3019.50826444 | −3019.51 |

Совпадение показывает, что расхождение термодинамики не вызвано другой геометрией, знаком взаимодействия или cutoff.

## Тесты проекта

Запущены три варианта: существующая OpenMP-сборка, существующая serial-сборка и новая Release/OpenMP-сборка. Во всех вариантах прошли одинаковые восемь тестов.

| Тест | Результат | Что проверяется |
|---|---|---|
| `pair_formula` | PASS | знак, угловой множитель, `1/r³` |
| `minimum_image` | PASS | 3D minimum image и открытый `z` для slab |
| `csv_geometry_ini` | PASS | геометрия, INI и приоритет CLI |
| `backends_incremental` | PASS | dense=CSR при полном cutoff, `ΔE`, поля после одного flip |
| `energy_grid_windows` | PASS | границы сетки и overlap окон |
| `walker_checkpoint` | PASS | histogram update и восстановление checkpoint |
| `refinement_transition` | PASS | переход WL → `1/t` на однобиновой системе |
| `exact_thermo` | PASS | число exact-состояний и конечность термодинамики |

Команды:

```powershell
.\build\wl_tests.exe
.\build-serial\wl_tests.exe
& "$env:TEMP\wl-audit-release-20260715\wl_tests.exe"
```

Не покрыты: длительный дрейф инкрементальной энергии, энергии точно на границе бина, полнота множества активных бинов, количественная точность WL против exact DOS, несовместимость overlap-окон, корректность bootstrap uncertainty и MPI replica exchange.

## Exact DOS против Wang–Landau

Использованы открытые решётки 2×2×1 (`N=4`) и 2×2×2 (`N=8`), диапазон `[-20,20]`, четыре walkers, seeds 1–5. Exact DOS получен полным перебором. Основная ширина бина — 0.25; дополнительно проверены 0.125 и 0.5.

Для `final_factor=10⁻⁶` все прогоны достигли лимита 2 млн попыток на walker без сходимости. Для получения завершённой контрольной серии использован `final_factor=10⁻⁵`. Система `N=4` сошлась при 400 тыс. попыток на walker; `N=8` не сошлась даже при 5 млн попыток на walker.

Пример команды:

```powershell
wl_run.exe --nx 2 --ny 2 --nz 1 --periodic false `
  --emin -20 --emax 20 --bin-width 0.25 `
  --windows 1 --walkers 4 --flatness 0.8 --min-visits 100 `
  --final-factor 1e-5 --max-attempts 2000000 `
  --complete-range true --temperatures 0.5,1,2,5 --seed 1
```

Ошибка `log g` вычислялась после удаления оптимальной аддитивной константы и только на бинах с ненулевым exact DOS.

### 2×2×1, сходившиеся прогоны

| Seed | RMSE `log g` | MAE | Максимум | Покрытие exact-бинов | Макс. `|ΔU|` | Макс. `|Δ(C/N)|` |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 0.000550 | 0.000519 | 0.000735 | 4/4 | 0.000088 | 0.000021 |
| 2 | 0.002107 | 0.001834 | 0.003272 | 4/4 | 0.001445 | 0.000493 |
| 3 | 0.001620 | 0.001504 | 0.002355 | 4/4 | 0.002526 | 0.000782 |
| 4 | 0.001895 | 0.001678 | 0.002917 | 4/4 | 0.001837 | 0.000326 |
| 5 | 0.002244 | 0.001925 | 0.003850 | 4/4 | 0.003929 | 0.001168 |

Средний RMSE равен 0.001683, стандартное отклонение между seeds — 0.000676. Нормировка даёт `Σg=16`. Повтор seed 1 сформировал побитово идентичный DOS-файл, то есть serial/OpenMP-расписание walkers в этой конфигурации воспроизводимо.

Термодинамика для seed 1:

| `T` | `U_exact` | `U_WL` | `(C/N)_exact` | `(C/N)_WL` |
|---:|---:|---:|---:|---:|
| 0.5 | −3.340167 | −3.340158 | 0.104044 | 0.104066 |
| 1.0 | −2.752852 | −2.752823 | 0.397246 | 0.397231 |
| 2.0 | −1.585989 | −1.586022 | 0.180507 | 0.180506 |
| 5.0 | −0.671126 | −0.671039 | 0.032784 | 0.032795 |

Этот тест подтверждает, что базовая acceptance-формула и обновление DOS способны давать правильный результат на простой системе без проблемных границ энергии.

### 2×2×2, несходившиеся прогоны

| Seed | RMSE `log g` | MAE | Максимум | Покрытие exact-бинов | Посещённые лишние бины | Макс. `|ΔU|` | Макс. `|Δ(C/N)|` |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1.363 | 1.105 | 3.695 | 17/17 | 5 | 2.154 | 0.326 |
| 2 | 2.507 | 2.081 | 6.166 | 17/17 | 5 | 5.594 | 1.016 |
| 3 | 2.106 | 1.700 | 4.546 | 17/17 | 5 | 4.762 | 1.349 |
| 4 | 3.775 | 2.965 | 9.591 | 17/17 | 5 | 6.230 | 2.987 |
| 5 | 2.490 | 2.023 | 5.208 | 17/17 | 5 | 5.573 | 1.348 |

Средний RMSE равен 2.448. Нормировка формально даёт `Σg=256`, но это следствие `complete_range=true`, а не подтверждение правильной формы DOS.

Для seed 1:

| `T` | `U_exact` | `U_WL` | `(C/N)_exact` | `(C/N)_WL` |
|---:|---:|---:|---:|---:|
| 0.5 | −13.120156 | −13.122602 | 0.009681 | 0.006681 |
| 1.0 | −12.755083 | −12.344272 | 0.221390 | 0.547617 |
| 2.0 | −9.463573 | −7.309413 | 0.433227 | 0.370370 |
| 5.0 | −4.274227 | −3.870426 | 0.098908 | 0.063322 |

### Энергии на границах бинов

Exact DOS имеет занятые бины 48, 64, 80, 96 и 112, соответствующие энергиям около −8, −4, 0, 4 и 8. WL дополнительно посещает соседние снизу бины 47, 63, 79, 95 и 111. Их histogram составляет примерно 6–9 тыс. посещений против примерно 1.17 млн на обычном активном бине; оценка `log g` этих ложных бинов ниже основной примерно на `1.45×10⁵`, поэтому их каноническая масса практически нулевая.

Это характерно для различий округления между полной суммой энергии и многократным накоплением `energy += ΔE` в непосредственной близости к границе `floor((E-Emin)/width)`. Редкие бины получают `active=1`, после чего участвуют в flatness-проверке и из-за малого histogram не позволяют завершить итерацию.

**Классификация: существенное.** Требуется стабильная политика биннинга около границ и длительный тест инкрементальной энергии против полного пересчёта.

### Чувствительность к ширине бина

| Система | Ширина | RMSE `log g` | Макс. `|ΔU|` | Макс. `|Δ(C/N)|` | Сходимость |
|---|---:|---:|---:|---:|---|
| 2×2×1 | 0.125 | 0.000550 | 0.000087 | 0.000024 | да |
| 2×2×1 | 0.25 | 0.000550 | 0.000088 | 0.000021 | да |
| 2×2×1 | 0.5 | 0.000550 | 0.000054 | 0.000035 | да |
| 2×2×2 | 0.125 | 1.363 | 2.192 | 0.314 | нет |
| 2×2×2 | 0.25 | 1.363 | 2.154 | 0.326 | нет |
| 2×2×2 | 0.5 | 1.363 | 2.164 | 0.323 | нет |

В этом наборе изменение ширины не устраняет проблему: критические энергии остаются согласованы с границами сетки.

## Диагностика Apamea DOS

Метаданные текущего запуска:

| Параметр | Значение |
|---|---:|
| Попытки | 800 000 000 |
| Принятые flips | 426 916 173 (53.36%) |
| Replica exchanges | 139 878 / 350 000 (39.97%) |
| Окна / walkers | 8 / 10 на окно |
| Final factor | `10⁻⁸` |
| Сходимость | **нет** |

Histogram и bootstrap standard error резко неоднородны:

| Окно | Бины | Нулевые | Медиана histogram | Медиана SE | Максимум SE |
|---:|---:|---:|---:|---:|---:|
| 0 | 1264 | 29 | 86 074 | 15.582 | 649.408 |
| 1 | 1264 | 0 | 120 | 0.222 | 0.270 |
| 2 | 1264 | 0 | 5 516 | 0.040 | 0.067 |
| 3 | 1264 | 0 | 7 908 | 0.018 | 0.030 |
| 4 | 1264 | 0 | 5 751 | 0.025 | 0.047 |
| 5 | 1264 | 0 | 8 423 | 0.089 | 0.353 |
| 6 | 1264 | 0 | 10 661 | 1.325 | 9.169 |
| 7 | 1264 | 49 | 87 756 | 8.631 | 578.089 |

Окно 1 имеет на два порядка меньше посещений, чем крайние окна. Сшивать эти оценки как равноценные нельзя.

Алгоритм выбирает join по минимальному локальному расхождению производных. Повторение этой процедуры показало:

| Стык окон | Энергия join | RMSE формы в overlap | Максимальное расхождение `log g` |
|---|---:|---:|---:|
| 0–1 | −645 | 161.0 | 446.6 |
| 1–2 | −313 | 52.7 | 141.4 |
| 2–3 | 297 | 12.0 | 43.8 |
| 3–4 | −471 | 0.088 | 0.271 |
| 4–5 | 505 | 0.518 | 1.806 |
| 5–6 | 1471 | 31.8 | 89.9 |
| 6–7 | 2103 | 177.2 | 547.7 |

У отдельных join локальный slope mismatch почти нулевой, но глобальная форма overlap расходится на сотни единиц. Минимум локальной производной поэтому не является достаточным критерием совместимости фрагментов.

Сшитый DOS содержит 3475 конечных значений `log g`, хотя 78 соответствующих histogram равны нулю. Это происходит потому, что непосещённые элементы инициализируются нулём, а сшивка не сохраняет маску физически доступных бинов.

## Сравнение Apamea с Metropolis

`apamea_N720_0.out` содержит 104 независимых Metropolis-точки с 10 000 heatup и 1 000 000 compute steps на температуру. Это стохастический независимый референс, а не точное решение.

Сравнивались `U(T)` и `C(T)/N` при всех 104 температурах.

| Метрика | `U` | `C/N` |
|---|---:|---:|
| MAE | 894.907 | 0.17237 |
| RMSE | 900.561 | 0.29452 |
| Максимальная абсолютная ошибка | 1008.532 | 1.19926 |

Максимальная ошибка энергии наблюдается около `T=4.890`: WL даёт `U=−1256.63`, Metropolis `U=−2265.16`.

| Контрольная точка | WL | Metropolis |
|---|---:|---:|
| `U(T=1)` | −2343.518 | −3009.204 |
| `C/N(T=1)` | 0.13858 | 0.06229 |
| Положение максимума `C/N` | `T=1.09354` | `T=1.78825` |
| Максимум `C/N` | 1.28908 | 0.37943 |

Расхождение систематическое по всему диапазону: при `T=10` WL всё ещё даёт `U=−716.20`, а референс `−1449.15`. Оно намного превышает статистический шум Metropolis и заявленные локальные standard errors DOS.

## Алгоритмические риски

### Критические

1. **Несходившийся Apamea представлен как полный набор термодинамических CSV.** Метаданные честно содержат `converged=false`, но `wl_run` всё равно сшивает DOS и вычисляет observables. Эти файлы легко принять за результат.
2. **Оконные DOS статистически несовместимы.** Стыки с расхождением сотни единиц `log g` должны приводить к отказу анализа, а не к выбору локально удобной точки.
3. **Текущая Apamea-термодинамика не воспроизводит независимый расчёт.** Ошибки меняют энергию и положение/высоту пика теплоёмкости, то есть физические выводы.

### Существенные

1. `flat()` пропускает все `active_[i]==0` (`src/wang_landau.cpp:157–171`). Доступный, но ещё не найденный уровень не мешает преждевременной сходимости; редкий ложный активный бин, наоборот, может навсегда блокировать flatness.
2. `EnergyGrid::index` использует строгий `floor` без политики допуска (`src/wang_landau.cpp:24–30`), а walker не выполняет периодическую сверку `energy_` с полной суммой.
3. Bootstrap в `src/parallel.cpp:19–46` ресемплирует walkers, чьи DOS периодически принудительно усредняются. Они не являются независимыми репликами, поэтому эта величина не оценивает полный bias и корреляции WL.
4. `complete_range=false` нормирует DOS только условием `max(log_g)=0`. Аддитивная константа неизвестна: `U` и `C` инвариантны к ней при достаточном диапазоне, но абсолютные `log Z`, `F` и `S` не определены. Отрицательная `S≈−587` при `T=1` в текущем CSV — прямой симптом произвольной нормировки.
5. Неполный энергетический диапазон может искажать даже `U` и `C`, если обрезана канонически значимая масса; код это не диагностирует.

### Малые и непроверенные

- Пользовательские интервалы задаются в MCS и разрешаются в flip-attempts после загрузки геометрии; внутреннее время `1/t` остаётся счётчиком обновлений DOS.
- Нет теста длительной последовательности случайных flips с периодическим полным пересчётом энергии и полей.
- Нет количественного теста stitching на искусственных фрагментах с известным DOS.
- MPI отсутствовал в окружении, поэтому multi-rank exchange и сбор фрагментов экспериментально не проверены.
- Minimum-image/cutoff соответствует локальному Apamea-референсу, но не моделирует бесконечную дипольную решётку методом Ewald.

## Необходимые исправления до научного использования

1. Ввести устойчивую политику энергетических границ: допуск/квантование относительно сетки либо периодическую полную рекалибровку энергии; добавить тесты точных границ и длинного дрейфа.
2. Разделить «физически доступные» и «посещённые» бины. Flatness должен проверять заданную/надёжно открытую маску доступности, а численные артефакты не должны расширять её автоматически.
3. Записывать по каждому walker и окну конечную стадию, factor, число активных бинов и причину остановки. Не строить итоговую термодинамику по несходившимся фрагментам без явного режима `--force`.
4. Добавить критерий качества overlap: RMSE/максимум после выравнивания, согласие slope на интервале и отказ при превышении порога.
5. Оценивать uncertainty по действительно независимым полным запускам/seeds либо корректным block/jackknife-методом; не интерпретировать текущий bootstrap как полную ошибку DOS.
6. Для `complete_range=false` помечать `log Z`, `F`, `S` как относительные/неопределённые или не выводить их. Проверять чувствительность `U,C` к расширению диапазона.
7. Добавить регрессионные exact-vs-WL тесты для 2×2×1 и 2×2×2, включая критерии RMSE DOS и термодинамики.
8. После исправлений заново выполнить Apamea несколькими независимыми seeds до фактического `converged=true` и сравнить между seeds и с Metropolis до интерпретации фазовых особенностей.

## Итог

Базовая физика пары и инкрементальное Monte Carlo-ядро заслуживают оценки **«корректно»**. Малый сходившийся пример 2×2×1 подтверждает высокую достижимую точность и воспроизводимость. Однако управление энергетическими бинами, критерий flatness, контроль совместимости окон и оценка uncertainty пока недостаточны для надёжного REWL на сложной системе.

**Текущий Apamea DOS и производные `U(T)`, `C(T)`, `F(T)`, `S(T)` использовать в научных выводах нельзя.** После исправления алгоритмических проблем необходим новый сходившийся ансамбль независимых запусков.
